# utils/

Standalone helper functions that don't belong in `lib/`.

| File | What it does |
|---|---|
| `toast.ts` | Thin wrappers around `sonner`'s `toast()` — exports `showSuccess`, `showError`, `showLoading`, and `dismissToast` with consistent styling. |
