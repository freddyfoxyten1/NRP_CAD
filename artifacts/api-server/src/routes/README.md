# routes/

Each file exports a single Express `Router` covering one API domain.
All routers are registered in `routes/index.ts`, which is imported by `app.ts`.

See `../README.md` for a full route-path table.
